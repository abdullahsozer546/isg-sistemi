{
  "name": "isg-sistemi",
  "version": "1.0.0",
  "description": "ISG Animasyon ve Eğitim Sistemi",
  "scripts": {
    "install": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node index.js"
  }
}
